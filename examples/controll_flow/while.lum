# EXPECT:
# block 0
# block 1
# block 2
# short 0
# short 1
# short 2
# loop 0
# loop 1
# loop 2
# after loop
# 0

def next(label Str, value Int) Int {
    OS.println(label + " " + value)
    value + 1
}

def main() Int {
    var count Int = 0
    while count < 3 {
        OS.println("block " + count)
        count += 1
    }

    var shortCount Int = 0
    while shortCount < 3 {
        shortCount := next("short", shortCount)
    }

    var loopCount Int = 0
    while true {
        OS.println("loop " + loopCount)
        loopCount += 1
        if loopCount == 3 {
            break
        }
    }

    while true {
        break
    }
    OS.println("after loop")

    0
}
