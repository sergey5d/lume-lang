# EXPECT:
# 42-hello
# 0

class Amount {
    count Int
    label Str
}

def main() Int {
    amount Amount = Amount(42, "hello")
    println(match amount {
        case Amount(count, label) => count + "-" + label
    })
    0
}
