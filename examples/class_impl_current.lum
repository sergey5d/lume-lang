# SKIP
#
# Current inline class-method shape, kept as a comparison artifact against the
# separated `class` + `impl` examples.

class Person with Named, Aged {
    firstName Str
    lastName Str
    age Int
    city Str

    hidden var archived Bool = false
    hidden var internalScore Int = 0

    new(fullName Str, age Int, city Str) {
        parts = fullName.split(" ")
        this.firstName = parts.get(0).getOr("")
        this.lastName = parts.get(1).getOr("")
        this.age = age
        this.city = city
    }

    new(firstName Str, lastName Str) {
        this.firstName = firstName
        this.lastName = lastName
        this.age = 18
        this.city = "unknown"
    }

    fullName() Str = this.firstName + " " + this.lastName

    isAdult() Bool = this.age >= 18

    moveTo(newCity Str) Unit {
        this.city := newCity
    }

    celebrateBirthday() Unit {
        this.age += 1
        this.internalScore += 10
    }

    archive() Unit {
        this.archived := true
    }

    debugLabel() Str =
        fullName() + "@" + this.city + ":" + this.age + ":" + this.internalScore + ":" + this.archived
}
