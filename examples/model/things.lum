# IGNORE

module things

interface Named {
    label() Str
}

class A with Named {

    label() Str = "A"
}


class B with Named {

    label() Str = "B"
}


object C {

    bump(value Int) Int = value + 1

    print(value Int) Int = value + 10

    printLn(value Int) Int = value + 100
}
