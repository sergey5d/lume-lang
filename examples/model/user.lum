# EXPECT:
# Ada
# 36

module user

class Person {
    name Str
    age Int
}

impl Person {
    def name() Str = this.name

    def age() Int = this.age
}

def main() Unit {
    person = Person { name: "Ada", age: 36 }
    println(person.name())
    println(person.age())
}
