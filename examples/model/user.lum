# EXPECT:
# Ada
# 36

module user

class Person {
    name Str
    age Int

    name() Str = this.name

    age() Int = this.age
}


main() Unit {
    person = Person { name: "Ada", age: 36 }
    println(person.name())
    println(person.age())
}
