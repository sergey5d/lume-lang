# FAIL_REGEX:
# .*empty brace construction for 'EmptyMap' is ambiguous between field construction and keyed construction; use 'EmptyMap\(\)' or 'EmptyMap.keyed\(\[\]\)'.*

class EmptyMap {}

single EmptyMap {}

impl single EmptyMap {
    keyed(entries [(Str, Int)]) EmptyMap {
        EmptyMap()
    }
}

def main() Unit {
    value EmptyMap = EmptyMap {}
}
