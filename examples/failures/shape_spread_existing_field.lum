# FAIL_REGEX:
# .*shape field 'age' already exists.*use ':<' to update existing fields.*

def main() Unit {
    user = { name: "Ada", age: 41 }
    updated = {
        ...user
        age: 42
    }
}
