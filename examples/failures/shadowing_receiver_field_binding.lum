# FAIL_REGEX:
# .*binding 'count' shadows a class field; use a different name.*

class Counter {
    count Int
}

impl Counter {
    def bad() Int {
        count = 1
        count
    }
}

def main() Unit {}
