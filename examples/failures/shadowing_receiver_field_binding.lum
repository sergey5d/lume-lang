# FAIL_REGEX:
# .*binding 'count' shadows a class field; use a different name.*

class Counter {
    count Int
}

impl Counter {
    bad() Int {
        count = 1
        count
    }
}

main() Unit {}
