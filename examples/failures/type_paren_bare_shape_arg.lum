# FAIL_REGEX:
# constructor syntax for 'MixedProfile' does not accept anonymous shape arguments in '\(\.\.\.\)'; use construction fields in braces or positional values directly

class MixedProfile {
    name Str
    age Int
}

def main() Unit {
    _ MixedProfile = MixedProfile({
        name: "Liam"
        age: 8
    })
}
