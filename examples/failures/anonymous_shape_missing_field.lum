# FAIL_REGEX:
# invalid_argument_type at .*: cannot pass \{name Str\} to parameter of type \{name Str, age Int\}

describe(user { name Str, age Int }) Str =
    user.name

main() Unit {
    value = { name: "Ana" }
    println(describe(value))
}
