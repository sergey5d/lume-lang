# FAIL_REGEX:
# .*binding 'count' shadows a class field; use a different name.*

class Counter {
    count Int
}

impl Counter {
    bad() Int {
        let Some(count) = Some(1) else return this.count
        this.count
    }
}

main() Unit {}
