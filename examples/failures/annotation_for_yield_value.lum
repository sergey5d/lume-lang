# FAIL_REGEX:
# .*annotation arguments must be literals, stable constants, or constant expressions.*

annotation Flag {
    values [Int]
}

@Flag { values: for item <- [1, 2] yield { item } }
main() Unit {}
