# FAIL_REGEX:
# .*guard binding syntax was removed; use 'let PATTERN = value else \.\.\.' for recoverable refutable bindings.*

main(value Option[Int]) Int {
    guard Some(item) = value else return 0
    item
}
