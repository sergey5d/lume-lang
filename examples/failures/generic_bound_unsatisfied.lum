# FAIL_REGEX:
# .*type 'Plain' does not satisfy generic bound 'Callable'.*

interface Callable {
    call() Unit
}

class Plain {}

invoke[T with Callable](value T) Unit = value.call()

main() Unit = invoke(Plain {})
