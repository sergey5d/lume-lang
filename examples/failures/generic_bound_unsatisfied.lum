# FAIL_REGEX:
# .*type 'Plain' does not satisfy generic bound 'Callable'.*

interface Callable {
    def call() Unit
}

class Plain {}

def invoke[T with Callable](value T) Unit = value.call()

def main() Unit = invoke(Plain {})
