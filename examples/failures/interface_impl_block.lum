# FAIL_REGEX:
# .*impl blocks cannot declare methods; move 'other' into the 'Named' declaration body.*

interface Named {
    name() Str = "named"
}

impl Named {
    other() Str = "other"
}

main() Int = 0
