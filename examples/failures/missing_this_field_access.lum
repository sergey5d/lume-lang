# FAIL_REGEX:
# undefined_name at .*: undefined name 'count'; if you meant the field, write 'this.count'

class Counter {
    count Int
}

impl Counter {
    def value() Int = count
}

def main() Int = 0
