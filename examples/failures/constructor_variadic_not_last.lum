# FAIL_REGEX:
# variadic constructor parameter must be last

class Bad {
    items [Int]
    suffix Int
}

impl Bad {
    new(vararg items [Int], suffix Int) {
        this.items = items
        this.suffix = suffix
    }
}

def main() Unit {
    _ Bad = Bad(1, 2, 3)
}
