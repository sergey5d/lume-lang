# FAIL_REGEX:
# variadic constructor parameter must be last

class Bad {
    items [Int]
    suffix Int

    new(items [Int] vararg, suffix Int) {
        this.items = items
        this.suffix = suffix
    }
}


main() Unit {
    _ Bad = Bad(1, 2, 3)
}
