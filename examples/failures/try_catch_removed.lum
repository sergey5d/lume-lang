# FAIL_REGEX:
# .*try catch syntax was removed; transform the source before try.*

main() Result[Int, Str] {
    result Result[Int, Str] = Err("bad")
    value = try result catch err => "mapped"
    Ok(value)
}
