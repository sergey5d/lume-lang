# FAIL_REGEX:
# invalid_field_initializer at .*: field initializer cannot call instance method 'makePrices'; move this logic to 'new\(\)'

class AssetPrices {
    assets [Str] = this.makePrices()

    makePrices() [Str] = []
}
