# FAIL_REGEX:
# .*postfix match is not supported; use 'match value \{ \.\.\. \}'.*

main(flag Bool) Int {
    value = flag match {
        case true => 1
        case false => 0
    }

    return value
}
