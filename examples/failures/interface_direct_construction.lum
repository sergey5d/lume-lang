# FAIL_REGEX:
# .*interface 'Reader' cannot be constructed directly; use 'object with Reader \{ ... \}' and define all required members.*

interface Reader {
    def read() Str
}

def main() Unit {
    reader = Reader()
    alsoReader = Reader {}
}
