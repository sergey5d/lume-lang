# FAIL_REGEX:
# .*constructor delegation must be the entire expression body.*

class User {
    name Str

    new(name Str) {
        this {
            name: name
        }
    }
}


def main() Unit = ()
