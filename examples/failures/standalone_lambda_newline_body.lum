# FAIL_REGEX:
# .*lambda body accepts one statement or expression; use '\{ \.\.\. \}' for multiple statements.*

def main() Unit {
    mapper = item ->
        next = item + 1
        next * 2

    println(mapper(4))
}
