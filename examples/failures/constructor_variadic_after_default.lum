# FAIL_REGEX:
# variadic constructor parameter cannot follow defaulted parameters

class Bad {
    items [Int]
}

impl Bad {
    new {
        prefix Int = 0
        items [Int] vararg
    } {
        this.items = items
    }
}

def main() Unit {
    _ Bad = Bad(1, 2, 3)
}
