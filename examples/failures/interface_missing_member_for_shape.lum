# FAIL_REGEX:
# .*shape 'NamedShape' declares interface 'Labeled' but does not implement required method 'label'.*

interface Labeled {
    label() Str
}

shape NamedShape with Labeled {
    name Str
}

main() Unit {
    named Labeled = NamedShape { name: "bad" }
}
