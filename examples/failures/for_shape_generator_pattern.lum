# FAIL_REGEX:
# .*for generator must bind a plain identifier or '_' before '<-'.*

shape User {
    name Str
    age Int
}

def main() Unit {
    users [User] = [User("Ada", 37)]

    for { name, age } <- users {
        println(name, age)
    }
}
