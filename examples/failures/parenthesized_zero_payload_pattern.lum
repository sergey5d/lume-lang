# FAIL_REGEX:
# .*empty parenthesized pattern 'None\(\)' is not supported; use the bare case name for a zero-payload enum case.*

def main(value Option[Int]) Int = match value {
    case Some(value) => value
    case None() => 0
}
