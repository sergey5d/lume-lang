# FAIL_REGEX:
# .*constructors use `new\(params\).*replace `new \{ \.\.\. \}` with `new\(\.\.\.\)`.*

class User {
    name Str

    new {
        name Str
    } {
        this.name = name
    }
}


main() Unit = ()
