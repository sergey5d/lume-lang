# FAIL_REGEX:
# .*annotation arguments must be literals, stable constants, or constant expressions.*

class User {
    name Str
}

annotation Owner {
    value User
}

@Owner { value: User("Ada") }
main() Unit {}
