# FAIL_REGEX:
# .*\.-> lifted access operator requires a lifted receiver, but receiver has type 'User'; use '\.' for ordinary member access.*

class User {
    name Str
}

main() Unit {
    user = User { name: "Ada" }
    name = user.->name
}
