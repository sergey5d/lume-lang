# FAIL_REGEX:
# .*annotation arguments must be literals, stable constants, or constant expressions.*

annotation Route {
    path Str
}

makePath() Str = "/runtime"

@Route { path: makePath() }
main() Unit {}
