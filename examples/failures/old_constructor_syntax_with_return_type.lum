# FAIL_REGEX:
# .*constructors use .*replace .*def new.*

class User {
    name Str
    age Int
}

impl User {
    def new(name Str, age Int) User {
        User {
            name: name
            age: age
        }
    }
}

def main() Unit = ()
