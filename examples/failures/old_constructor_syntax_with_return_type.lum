# FAIL_REGEX:
# .*constructors omit 'def'.*

class User {
    name Str
    age Int

    def new(name Str, age Int) User {
        User {
            name: name
            age: age
        }
    }
}


main() Unit = ()
