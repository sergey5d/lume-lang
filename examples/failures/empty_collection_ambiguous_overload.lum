# FAIL_REGEX:
# .*empty collection.*matches multiple array/map overloads.*

class Consumer {}

impl Consumer {
    new(values [Str]) {}
    new(values [Str : Int]) {}
}

def main() Unit {
    consumer = Consumer([])
}
