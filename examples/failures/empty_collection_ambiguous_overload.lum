# FAIL_REGEX:
# .*empty collection.*matches multiple vector/map overloads.*

class Consumer {

    new(values [Str]) {}
    new(values [Str : Int]) {}
}


def main() Unit {
    consumer = Consumer([])
}
