# FAIL_REGEX:
# no_matching_overload at .*: constructor syntax for 'MixedProfile' does not accept anonymous record arguments in '\(\.\.\.\)'; use 'MixedProfile \{ \.\.\. \}'

class MixedProfile {
    name Str
    age Int
}

def main() Unit {
    _ MixedProfile = MixedProfile({
        name: "Liam"
        age: 8
    })
}
