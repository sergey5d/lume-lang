# FAIL_REGEX:
# class 'MixedProfile' uses brace construction; write 'MixedProfile \{ \.\.\. \}'

class MixedProfile {
    name Str
    age Int
}

def main() Unit {
    _ MixedProfile = MixedProfile({
        name: "Liam"
        age: 8
    })
}
