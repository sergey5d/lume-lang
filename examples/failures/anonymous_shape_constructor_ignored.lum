# FAIL_REGEX:
# constructor syntax for 'SecretBadge' does not accept anonymous shape arguments in '\(\.\.\.\)'; use construction fields in braces or positional values directly

class SecretBadge {
    name Str
    hidden code Int

    new(name Str) {
        this.name = name
        this.code = 99
    }
}


main() Unit {
    _ SecretBadge = SecretBadge({
        name: "Mia"
    })
}
