# FAIL_REGEX: named arguments cannot target variadic constructor parameters

class Path {
    segments [Str]
}

impl Path {
    new {
        segments [Str] vararg
    } {
        this.segments = segments
    }
}

path Path = Path { segments: ["usr"] }

