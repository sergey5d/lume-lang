# FAIL_REGEX:
# .*for yield clauses do not support 'guard'.*

def main() Unit {
    values [Option[Int]] = [Some(1)]

    mapped = for {
        maybe <- values
        guard Some(value) = maybe else {
            value = 0
        }
    } yield value

    println(mapped.size())
}
