# FAIL_REGEX:
# .*field 'name' is provided by both 'left' and 'right'.*select a value explicitly or use 'override' on one spread.*

def main() Unit {
    left = { name: "Ada", age: 42 }
    right = { name: "Bob" }
    value = {
        ...left
        ...right
    }
}
