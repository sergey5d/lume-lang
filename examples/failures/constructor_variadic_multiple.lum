# FAIL_REGEX:
# only one variadic constructor parameter is allowed

class Bad {
    left [Int]
    right [Int]

    new(left [Int] vararg, right [Int] vararg) {
        this.left = left
        this.right = right
    }
}
