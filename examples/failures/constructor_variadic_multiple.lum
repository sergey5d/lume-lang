# FAIL_REGEX:
# only one variadic constructor parameter is allowed

class Bad {
    left [Int]
    right [Int]
}

impl Bad {
    new(vararg left [Int], vararg right [Int]) {
        this.left = left
        this.right = right
    }
}
