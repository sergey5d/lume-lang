# FAIL_REGEX:
# .*plain 'let' pattern may fail for value of type 'Vector\[Int\]'; add an 'else' fallback.*

main() Unit {
    values [Int] = [1, 2]
    let [left, right] = values

    println(left + right)
}
