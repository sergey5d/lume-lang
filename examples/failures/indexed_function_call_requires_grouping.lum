# FAIL_REGEX:
# .*callee\[\.\.\.\]\(\.\.\.\) is explicit generic application syntax; to call an indexed function value, write '\(callee\[key\]\)\(\)'.*

def main() Unit {
    handlers [fn() => Int] = [() => 26]
    println(handlers[0]())
}
