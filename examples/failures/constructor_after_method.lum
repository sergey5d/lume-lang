# FAIL_REGEX:
# .*constructors must appear before methods in impl 'User'.*

class User {
    name Str
}

impl User {
    def label() Str = this.name

    new(name Str) {
        this.name = name
    }
}
