# FAIL_REGEX:
# .*headless record pattern requires a concrete class, shape, anonymous shape, or enum value; got 'Named'.*

interface Named {
    def name() Str
}

def read(value Named) Str = match value {
    case { name } => name
    case _ => "unknown"
}
