# FAIL_REGEX:
# .*cannot infer reified type parameter 'Ann'.*

annotation Route {
    path Str
}

class Service {}

main() Unit {
    metadata Type[Service] = typeOf[Service]
    metadata.hasAnnotation()
}
