# FAIL_REGEX:
# .*for generator must bind a plain identifier or '_' before '<-'.*

def main() Unit {
    pairs List[(Int, Int)] = [(1, 2)]

    for (left, right) <- pairs {
        println(left + right)
    }
}
