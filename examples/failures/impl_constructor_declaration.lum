# FAIL_REGEX:
# .*impl blocks cannot declare constructors; move 'new' into the 'Counter' declaration body.*

class Counter {
    value Int
}

impl Counter {
    new(value Int) {
        this.value = value
    }
}
