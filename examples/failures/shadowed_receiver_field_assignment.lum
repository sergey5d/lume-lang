# FAIL_REGEX:
# .*class field 'count' is shadowed, use 'this.count'.*

class Counter {
    var count Int

    new(count Int) {
        this.count = count
    }

    def bad(count Int) Unit {
        count := count + 1
    }
}


def main() Unit {}
