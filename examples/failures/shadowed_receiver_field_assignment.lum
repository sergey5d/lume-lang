# FAIL_REGEX:
# .*class field 'count' is shadowed, use 'this.count'.*

class Counter {
    var count Int
}

impl Counter {
    new(count Int) {
        this.count = count
    }

    bad(count Int) Unit {
        count := count + 1
    }
}

main() Unit {}
