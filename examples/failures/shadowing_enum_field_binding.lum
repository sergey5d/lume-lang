# FAIL_REGEX:
# .*binding 'code' shadows an enum field; use a different name.*

enum Color {
    code Str

    case Red {
        code = "red"
    }

    bad() Str {
        code = "shadow"
        this.code
    }
}

main() Unit {}
