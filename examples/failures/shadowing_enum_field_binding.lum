# FAIL_REGEX:
# .*binding 'code' shadows an enum field; use a different name.*

enum Color {
    code Str

    case Red {
        code = "red"
    }

    def bad() Str {
        code = "shadow"
        this.code
    }
}

def main() Unit {}
