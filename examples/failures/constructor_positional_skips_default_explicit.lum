# FAIL_REGEX:
# positional construction for Article with 1 argument leaves required field 'title' unset.*

class Article {
    body Str
    title Str

    new(body Str = "body", title Str) {
        this.body = body
        this.title = title
    }
}


main() Unit {
    _ Article = Article("Intro")
}
