# FAIL_REGEX:
# .*annotation arguments must be literals, stable constants, or constant expressions.*

class Config {
    path Str = "/ordinary-object"
}

config Config = Config {}

annotation Route {
    path Str
}

@Route { path: config.path }
main() Unit {}
