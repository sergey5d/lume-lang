# FAIL_REGEX:
# class 'SecretBadge' uses brace construction; write 'SecretBadge \{ \.\.\. \}'

class SecretBadge {
    name Str
    hidden code Int
}

impl SecretBadge {
    new {
        name Str
    } {
        this.name = name
        this.code = 99
    }
}

def main() Unit {
    _ SecretBadge = SecretBadge({
        name: "Mia"
    })
}
