# FAIL_REGEX:
# no_matching_overload at .*: constructor syntax for 'SecretBadge' does not accept anonymous record arguments in '\(\.\.\.\)'; use 'SecretBadge \{ \.\.\. \}'

class SecretBadge {
    name Str
    hidden code Int
}

impl SecretBadge {
    def new(name Str) {
        this.name = name
        this.code = 99
    }
}

def main() Unit {
    _ SecretBadge = SecretBadge(class {
        name: "Mia"
    })
}
