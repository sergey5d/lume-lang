# FAIL_REGEX:
# no_matching_overload at .*: class 'SecretBadge' cannot use brace-based construction because it defines explicit constructors

class SecretBadge {
    name Str
    hidden code Int
}

impl SecretBadge {
    def new(name Str) {
        this.name = name
        this.code = 99
    }
}

def main() Unit {
    _ SecretBadge = SecretBadge(class {
        name = "Mia"
    })
}
