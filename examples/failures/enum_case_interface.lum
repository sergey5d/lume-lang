# FAIL_REGEX:
# .*enum case 'Outcome.Left' cannot implement interfaces; put 'with ...' on enum 'Outcome'.*

interface Labeled {
    label() Str
}

enum Outcome {
    case Left with Labeled {
        value Str
    }
}
