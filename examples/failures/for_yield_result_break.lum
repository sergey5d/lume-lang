# FAIL_REGEX:
# .*`break` inside `for \.\.\. yield` is only supported for iterable comprehensions; Result comprehensions have no early-exit state.*

main() Result[Int, Str] =
    for item <- Ok(1) yield {
        if item > 0 {
            break
        }

        item + 1
    }
