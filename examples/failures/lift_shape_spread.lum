# FAIL_REGEX:
# .*invalid_lift.*lift does not support shape spread entries yet; list lifted fields explicitly.*

def main() Unit {
    base = {
        id: Some(1)
    }
    value = lift {
        ...base
    }
}
