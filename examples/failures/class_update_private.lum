# FAIL_REGEX:
# invalid_shape_update at .*: update field 'secret' does not exist on left-hand shape

class SecretAmount {
    amount Int
    hidden secret Str = "hidden"
}

def main() Unit {
    value = SecretAmount(10)
    updated = value :< { secret: "changed" }
}
