# FAIL:
# pattern matches in 'if' require 'let'; use 'if let Pattern = value { ... }' or 'if let Pattern <- value { ... }'

def main() Unit {
    value Int? = Some(1)
    if Some(item) = value {
        println(item)
    }
}
