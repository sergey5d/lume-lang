# FAIL_REGEX:
# no_matching_overload at .*: class 'MixedProfile' requires named brace fields that match the visible class shape

class MixedProfile {
    name Str
    age Int
    hidden score Int = 5
}

def main() Unit {
    _ MixedProfile = MixedProfile {
        name = "Liam"
        age = 8
        score = 5
    }
}
