# FAIL_REGEX:
# .*constructor delegation uses `this \{ \.\.\. \}`; `new` only declares constructors.*

class User {
    name Str

    new(name Str) = new {
        name: name
    }
}


def main() Unit = ()
