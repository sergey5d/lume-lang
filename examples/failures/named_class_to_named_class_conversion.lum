# FAIL_REGEX:
# class 'Target' positional construction expects 2\.\.2 arguments, got 1

class Source {
    name Str
    age Int
}

class Target {
    name Str
    age Int
}

def main() Unit {
    source Source = Source {
        name: "Ada"
        age: 10
    }
    _ Target = Target(source)
}
