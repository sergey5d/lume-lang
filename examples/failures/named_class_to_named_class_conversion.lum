# FAIL_REGEX:
# class 'Target' uses brace construction; write 'Target \{ \.\.\. \}'

class Source {
    name Str
    age Int
}

class Target {
    name Str
    age Int
}

def main() Unit {
    source Source = Source {
        name: "Ada"
        age: 10
    }
    _ Target = Target(source)
}
