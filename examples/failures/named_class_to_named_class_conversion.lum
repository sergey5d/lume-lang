# FAIL_REGEX:
# call expects 2\.\.2 arguments, got 1

class Source {
    name Str
    age Int
}

class Target {
    name Str
    age Int
}

main() Unit {
    source Source = Source {
        name: "Ada"
        age: 10
    }
    _ Target = Target(source)
}
