# FAIL_REGEX:
# .*continue used outside of a loop.*

run(work fn() => Unit) Unit {
    work()
}

main() Unit {
    for maybe <- [Some(1)] {
        run { () =>
            value = maybe ?? continue
            println(value)
        }
    }
}
