# FAIL_REGEX:
# .*enum case 'Missing' does not accept call syntax; use 'Missing'.*

enum MaybeInt {
    case Missing
    case Present {
        value Int
    }
}

main() Unit {
    value MaybeInt = Missing()
}
