# FAIL_REGEX:
# .*argument has type 'Int' but parameter expects captured key type of Map\[_, Str\].*

def intStrMap() Map[Int, Str] = Map()

def main() Unit {
    keyErased Map[_, Str] = intStrMap()
    keyErased.put(1, "one")
}
