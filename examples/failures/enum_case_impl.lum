# FAIL_REGEX:
# enum cases cannot declare methods; move methods to enum 'Outcome'

enum Outcome {
    case Left {
        value Str
    }
}

impl Outcome.Left {
    describe() Str = this.value
}

main() Int = 0
