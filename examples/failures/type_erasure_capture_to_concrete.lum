# FAIL_REGEX:
# .*cannot assign value of type captured element type of Array\[_\] to binding 'concrete' of type 'SomeType'.*

class SomeType {
    value Int
}

def main() Unit {
    values [_] = [1, 2, 3]
    concrete SomeType = values[0]
}
