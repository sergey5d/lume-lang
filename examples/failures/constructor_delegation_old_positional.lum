# FAIL_REGEX:
# .*constructor delegation uses `this\(\.\.\.\)`; `new` only declares constructors.*

class User {
    name Str
    age Int
}

impl User {
    new(name Str, age Int) {
        this.name = name
        this.age = age
    }

    new(label Str) = new(label, 0)
}

main() Unit = ()
