# FAIL_REGEX:
# .*empty field pattern 'None \{\}' is redundant; use the bare zero-payload case 'None'.*

def main(maybe Option[Int]) Int = match maybe {
    case Some(value) => value
    case None {} => 0
}
