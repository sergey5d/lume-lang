# FAIL_REGEX:
# .*annotation arguments must be literals, stable constants, or constant expressions.*

annotation Route {
    path Str
}

object Routes {
    var health Str = "/mutable"
}

@Route { path: Routes.health }
main() Unit {}
