# FAIL_REGEX:
# .*trailing lambda syntax requires an explicit parameter arrow.*

class Runner {

    zero(f fn() => Int) Int = f()
}


main() Unit {
    runner = Runner {}
    println(runner.zero {
        26
    })
}
