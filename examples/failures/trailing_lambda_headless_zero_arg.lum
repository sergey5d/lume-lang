# FAIL_REGEX:
# .*trailing lambda syntax requires an explicit parameter arrow.*

class Runner {

    def zero(f fn() Int) Int = f()
}


def main() Unit {
    runner = Runner {}
    println(runner.zero {
        26
    })
}
