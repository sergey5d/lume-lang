# FAIL_REGEX:
# braces are for construction fields; use 'Type\(\.\.\.\)' for positional constructors

class Order {
    id Int
}

main() Option[Order] {
    Some {
        Order {
            id: 7
        }
    }
}
