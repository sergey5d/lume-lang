# FAIL_REGEX:
# use ':=' for reassignment; '=' is only for bindings and constructor initialization

class Counter {
    hidden var count Int = 0

    bump() Unit {
        this.count = 1
    }
}


main() Unit {
    Counter {}.bump()
}
