# FAIL_REGEX:
# .*expect binding syntax was removed; use 'let PATTERN = value else panic\(\.\.\.\)' for assertive refutable bindings.*

def main() Unit {
    expect item <- Some(5)
    println(item)
}
