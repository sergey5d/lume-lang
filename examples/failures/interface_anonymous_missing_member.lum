# FAIL_REGEX:
# .*anonymous implementation of interface 'Reader' is missing method 'close'.*

interface Reader {
    read() Str
    close() Unit
}

main() Unit {
    handler Reader = Reader {
        read() Str = "open"
    }
}
