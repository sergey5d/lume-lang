# FAIL_REGEX:
# .*argument has captured element type of Array\[_\], but parameter expects a different captured element type of Array\[_\].*

same[T](left T, right T) Unit {}

main() Unit {
    left [_] = [1]
    right [_] = [2]

    same(left[0], right[0])
}
