# FAIL_REGEX:
# .*argument has captured element type of List\[_\], but parameter expects a different captured element type of List\[_\].*

def same[T](left T, right T) Unit {}

def main() Unit {
    left List[_] = List(1)
    right List[_] = List(2)

    same(left[0], right[0])
}
