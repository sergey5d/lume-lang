# FAIL_REGEX:
# .*argument has type '_' but parameter expects '_'.*

def same[T](left T, right T) Unit {}

def main() Unit {
    left List[_] = List(1)
    right List[_] = List(2)

    same(left[0], right[0])
}
