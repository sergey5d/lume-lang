# EXPECT:
# A
# A
# B
# 11
# 112
# 110
# 0

module app

use model/things
use model/things/A
use model/things/A as AliasA
use model/things/{B as AliasB, Named}
use model/things/*
use model/things/C/*
use model/things/C/{printLn as printN, print}

main() Int {
    value A = A {}
    aliasA AliasA = AliasA {}
    valueB AliasB = AliasB {}
    named Named = value

    println(named.label())
    println(aliasA.label())
    println(valueB.label())
    println(bump(4) + things.C.bump(5))
    println(printN(12))
    println(print(100))
    return 0
}
