# EXPECT:
# 3
# 0
# 0

def acceptUnknownArray(values [_]) Unit {
    println(values.size())
}

def same[T](left T, right T) Unit {}

def intStrMap() [Int: Str] = []

def strIntMap() [Str: Int] = []

def main() Unit {
    values [_] = [1, 2, 3]
    keyErased [_: Str] = intStrMap()
    fullyErased [_: _] = strIntMap()

    first Any = values[0]
    captured = values[0]
    sameCapture = captured

    same(values[0], values[1])
    acceptUnknownArray(values)

    println(keyErased.size())
    println(fullyErased.size())
}
