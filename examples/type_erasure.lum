# EXPECT:
# 3
# 0
# 0

acceptUnknownArray(values [_]) Unit {
    println(values.size())
}

same[T](left T, right T) Unit {}

intStrMap() [Int: Str] = []

strIntMap() [Str: Int] = []

main() Unit {
    values [_] = [1, 2, 3]
    keyErased [_: Str] = intStrMap()
    fullyErased [_: _] = strIntMap()

    first Any = values[0]
    captured = values[0]
    sameCapture = captured

    same(values[0], values[1])
    acceptUnknownArray(values)

    println(keyErased.size())
    println(fullyErased.size())
}
