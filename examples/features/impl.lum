# EXPECT:
# impl hello Ada

class Greeter {
    name Str
}

impl Greeter {
    greet() Str =
        "hello " + this.name
}

main() Unit {
    greeter Greeter = Greeter { name: "Ada" }
    println("impl", greeter.greet())
}
