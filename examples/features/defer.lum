# EXPECT:
# start middle done
# body inner outer
# 0

basic() Unit {
    OS.print("start ")
    defer println("done")
    OS.print("middle ")
}

lifo() Unit {
    defer println("outer")
    defer OS.print("inner ")
    OS.print("body ")
}

main() Int {
    basic()
    lifo()
    0
}
