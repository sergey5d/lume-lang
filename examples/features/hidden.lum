# EXPECT:
# hidden secret

class SecretBox {
    hidden value Str = "secret"

    reveal() Str =
        this.value
}

main() Unit {
    box SecretBox = SecretBox {}
    println("hidden", box.reveal())
}
