# EXPECT:
# with Ada

interface Named {
    name() Str
}

class Person with Named {
    name Str
}

impl Person {
    name() Str =
        this.name
}

main() Unit {
    person Person = Person { name: "Ada" }
    println("with", person.name())
}
