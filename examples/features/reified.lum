# EXPECT:
# reified User

class User {
    name Str
}

def typeLabel[reified A](value A) Str {
    let name <- typeOf[A].name() else panic("expected type name")
    name
}

def main() Unit {
    user User = User { name: "Ada" }
    println("reified", typeLabel(user))
}
