# EXPECT:
# 6
# 7

def increment(value Int?) Int? = value.map(item => item + 1)

def main() Unit {
    shorthand Int? = Some(5)
    explicit Option[Int] = shorthand
    println(increment(explicit)!!)

    nested Option[Int?] = Some(Some(7))
    println(nested!!!!)
}
