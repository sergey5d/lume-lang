# EXPECT:
# yield 2
# yield 3
# yield 4
# size 3
# 0

main() Int {
	items = for item <- [1, 2, 3] yield {
		item + 1
	}

	for item <- items {
		println("yield " + item)
	}

	println("size " + items.size())
	0
}
