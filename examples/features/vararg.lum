# EXPECT:
# vararg 6

def sum(values [Int] vararg) Int {
    var total Int = 0

    for value <- values {
        total := total + value
    }

    total
}

def main() Unit {
    println("vararg", sum(1, 2, 3))
}
