# EXPECT:
# try 3

increment(value Option[Int]) Option[Int] {
    item = try value
    Some(item + 1)
}

main() Unit {
    let value <- increment(Some(2)) else panic("expected incremented value")
    println("try", value)
}
