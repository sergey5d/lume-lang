# EXPECT:
# interface Ada

interface Named {
    name() Str
}

class Person with Named {
    name Str
}

impl Person {
    name() Str =
        this.name
}

describe(value Named) Str =
    value.name()

main() Unit {
    person Person = Person { name: "Ada" }
    println("interface", describe(person))
}
