# EXPECT:
# token edge
# number 42

enum Token {
    case Start
    case Stop
    case Word {
        value Str
    }
    case Number {
        value Str
    }
}

describe(token Token) Str = {
    match token {
        case Token.Start | Token.Stop => "edge"
        case Word(value) | Number(value) => "number " + value
    }
}

main() Unit = {
    println("token", describe(Token.Start))
    println(describe(Token.Number("42")))
}
