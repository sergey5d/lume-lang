module bumper

class Counter {
    hidden var count Int
}

impl Counter {
    new(count Int) {
        this.count = count
    }

    bump(delta Int) Int {
        this.count += delta
        return this.count
    }
}

seed Int = 1

run() Int {
    c Counter = Counter(seed)
    return c.bump(2)
}
