# EXPECT:
# 7
# 0

object Ascending with Ordering[Int] {

    compare(left Int, right Int) Int = left - right
}


class Box[T with Ordering[T]] {
    value T
}

class Mapper {

    pick[X with Ordering[X]](value X) X = value
}


pick[T with Ordering[T]](value T) T = value

useBox(value Box[Int]) Int = value.value

main() Int {
    mapper Mapper = Mapper {}
    println(pick(3) + mapper.pick(4))
    0
}
