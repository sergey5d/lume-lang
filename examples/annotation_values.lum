# EXPECT:
# ok

module app

use model/annotation_values

annotation Route {
    path Str
}

@Route { path: annotation_values.routePath }
@Route { path: annotation_values.Routes.health }
def main() Unit {
    println("ok")
}
