# EXPECT:
# 15
# 16
# 31
# 0

main() Int {
    base = 10

    add(left Int, right Int) Int {
        left + right + base
    }

    double(value Int) Int = value * 2

    println(add(2, 3))
    println(double(8))

    combine(value Int) Int {
        bump(amount Int) Int = amount + 1
        add(bump(value), double(5))
    }

    println(combine(10))
    0
}
