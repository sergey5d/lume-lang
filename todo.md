# Language Design TODO

- Formalize the `<-` extraction model so custom collections and classes can participate.
- Settle operator declaration and implementation syntax.
- Settle indexed access semantics for `Vector`.
