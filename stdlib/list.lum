# PRELUDE_SKIP
# JAVA_SKIP
# INTERPRETER

interface List[T] with Iterable[T] {
    def append(value T) List[T]
    def add(value T) List[T]
    def addAll(values Iterable[T]) List[T]
    def map[X](f fn(T) => X) List[X]
    def flatMap[X](f fn(T) => List[X]) List[X]
    def filter(f fn(T) => Bool) List[T]
    def fold[X](initial X, f fn(X, T) => X) X
    def reduce[X](initial X, f fn(X, T) => X) X
    def reduce(f fn(T, T) => T) Option[T]
    def exists(f fn(T) => Bool) Bool
    def forAll(f fn(T) => Bool) Bool
    def sort(ordering Ordering[T]) List[T]
    def zip[X](other List[X]) List[(T, X)]
    def zipWithIndex() List[(T, Int)]
    def get(index Int) Option[T]
    def head() Option[T]
    def tail() List[T]
    def isEmpty() Bool
    def nonEmpty() Bool = !this.isEmpty()
    def remove(index Int) Option[T]
    def removeFirst() Option[T]
    def removeLast() Option[T]
    def size() Int
    def forEach(f fn(T) => Unit)

    def makeStr(separator Str) Str {
        var result Str = ""
        for idx <- Range(0, this.size()) {
            if let Some(item) = this.get(idx) {
                if idx > 0 {
                    result := result + separator
                }
                result := result + item
            }
        }
        result
    }
}
