# PRELUDE_SKIP
# JAVA_SKIP
# INTERPRETER

object OS with Printer {
    stdout Printer
    stderr Printer
}

impl object OS {
    def print(value [Str] vararg) Unit = ()
    def println(value [Str] vararg) Unit = ()
    def printf(format Str, value [Str] vararg) Unit = ()
}
