# PRELUDE_SKIP
# JAVA_SKIP
# INTERPRETER

object OS with Printer {
    stdout Printer
    stderr Printer
}

impl object OS {
    def print(vararg value [Str]) Unit = ()
    def println(vararg value [Str]) Unit = ()
    def printf(format Str, vararg value [Str]) Unit = ()
}
