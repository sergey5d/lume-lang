# PRELUDE_SKIP
# JAVA_SKIP
# INTERPRETER

interface Str {
    def size() Int
    def runeAt(index Int) Option[Rune]
    def expectRuneAt(index Int) Rune
    def split(separator Str) FixedArray[Str]
    def indexOf(part Str) Int
}
